@model IEnumerable<heshamhrms.Models.National_services_status>

@{
    ViewBag.Title = "حالة الخدمة الوطنية";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>حالة الخدمة الوطنية</h2>

<p>
    @Html.ActionLink("إنشاء جديد", "Create")
</p>
<table class="table table-striped">
    <thead class="tablehead">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.National_services_status_name)
            </th>
            <th>تعديل</th>
            <th>تفاصيل</th>
            <th>حذف</th>
        </tr>
        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.National_services_status_name)
                </td>
                <td>
                    @Html.ActionLink("تعديل", "Edit", new { id = item.National_services_status_id })
                </td>
                <td>
                    
                    @Html.ActionLink("تفاصيل", "Details", new { id = item.National_services_status_id })
                    
                </td>
                <td>
                    @Html.ActionLink("حذف", "Delete", new { id = item.National_services_status_id })
                </td>
            </tr>
        }

</table>

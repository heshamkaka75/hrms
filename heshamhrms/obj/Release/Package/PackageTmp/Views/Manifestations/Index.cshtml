@model IEnumerable<heshamhrms.Models.Manifestations>

@{
    ViewBag.Title = "إستيضاحات";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>إستيضاحات</h2>

<p>
    @Html.ActionLink("جديد", "Create")
</p>
<table class="table table-striped">
    <thead class="tablehead">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Manifestation_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employees.Employee_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manifestation_Reason)
            </th>
            <th>تعديل</th>
            <th>تفاصيل</th>
            <th>حذف</th>
        </tr>
        </thead>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Manifestation_date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Employees.Employee_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manifestation_Reason)
                </td>
                
                <td>
                    @Html.ActionLink("تعديل", "Edit", new { id = item.Manifestation_id }) 
                </td>
                <td>
                    
                    @Html.ActionLink("تفاصيل", "Details", new { id = item.Manifestation_id }) 
                    
                </td>
                <td>
                    @Html.ActionLink("حذف", "Delete", new { id = item.Manifestation_id })
                </td>
            </tr>
        }

</table>

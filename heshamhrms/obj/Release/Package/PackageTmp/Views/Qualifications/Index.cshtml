@model IEnumerable<heshamhrms.Models.Qualifications>

@{
    ViewBag.Title = "الشهادات الاكادمية";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>الشهادات الاكادمية</h2>

<p>
    @Html.ActionLink("إنشاء جديد", "Create")
</p>
<table class="table table-striped">
    <thead class="tablehead">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Qualification_name)
            </th>
            <th>تعديل</th>
            <th>تفاصيل</th>
            <th>حذف</th>
        </tr>
        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Qualification_name)
                </td>
                <td>
                    @Html.ActionLink("تعديل", "Edit", new { id = item.Qualification_id })
                </td>
                <td>
                    
                    @Html.ActionLink("تفاصيل", "Details", new { id = item.Qualification_id })
                    
                </td>
                <td>
                    @Html.ActionLink("حذف", "Delete", new { id = item.Qualification_id })
                </td>
            </tr>
        }

</table>

@model heshamhrms.Models.Courses_name

@{
    ViewBag.Title = "حذف دورة تدربية";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
    try
    {
        if (Session["username"].ToString() != "ADMIN")
        { Response.Redirect("~/Login/notauth"); }
    }
    catch { Response.Redirect("~/Login/home"); }
        
}

<h2>حذف دورة تدربية</h2>

<h3>هل تريد حذف هذه الدورة ؟</h3>
<div>
    
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Course_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Course_name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="حذف دورة" class="btn btn-danger" /> |
            @Html.ActionLink("الرجوع للقائمة", "Index")
        </div>
    }
</div>

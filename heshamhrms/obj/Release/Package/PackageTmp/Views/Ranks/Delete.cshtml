@model heshamhrms.Models.ranks

@{
    ViewBag.Title = "حذف درجة وظيفية";
    Layout = "~/Views/Shared/_MainLayout.cshtml";

    try
    {
        if (Session["username"].ToString() != "ADMIN")
        { Response.Redirect("~/Login/notauth"); }
    }
    catch { Response.Redirect("~/Login/home"); }
        
}

<h2>حذف درجة وظيفية</h2>

<h3>هل تريد حذف هذه الدرجة ؟؟؟</h3>
<div>
    
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.rank_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.rank_name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="حذف درجة وظيفية" class="btn btn-danger" /> |
            @Html.ActionLink("الرجوع الي القائمة", "Index")
        </div>
    }
</div>
